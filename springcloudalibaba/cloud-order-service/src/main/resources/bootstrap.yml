spring:
  application:
    name: cloud-order-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml #文件拓展格式
  profiles:
    active: dev
